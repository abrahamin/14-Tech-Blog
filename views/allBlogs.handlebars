<div class="blogs" style="width: 18rem;">
  {{#each blogs}}
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{User.username}} on {{format_date date_created}}</h6>
    <p class="card-text">{{body}}</p>
  </div>
  {{/each}}
</div>